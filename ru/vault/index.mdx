---
title: "MainVault"
description: "Депозиты, аллокация, консервативный NAV"
---

MainVault это слой капитала. Он принимает депозиты LP, выдаёт принципал funded-счетам, учитывает резерв билдера и проводит один путь close.

## Потоки

<CardGroup cols={3}>
  <Card title="Аллокация" icon="banknote">
    На активации волт кладёт принципал `A` на Hypercore. Вызывающий платит `R + P`
  </Card>
  <Card title="Учёт" icon="coins">
    `R` это активное обязательство билдера. Премия и подписка на стороне волта остаются в NAV
  </Card>
  <Card title="Сеттлмент" icon="rotate-ccw">
    Close забирает снапшот `Q`, неиспользованный резерв и атрибутированные клеймы. Неатрибутированный остаток на EVM возвращается в волт
  </Card>
</CardGroup>

## Консервативный NAV

```
валовые активы = USDC на EVM + totalAllocated

NAV = валовые
    − активный резерв билдеров
    − клейма возврата резерва
    − клейма выплат

цена share = NAV / число shares
```

Резерв, невыплаченные возвраты и невыплаченные payouts цену share не поднимают. Пока close не зафиксировал `Q`, депозиты и выходы идут по этому ончейн-NAV.

## Лимиты

| Параметр | Дефолт | Что режет |
|---|---|---|
| Макс. утилизация | 80% | `totalAllocated` к NAV |
| Один счёт | 10% / $20k | Один `A` |
| Размер счёта | $10 – $20,000 | Один `A` |
| Drawdown | 5% – 25% | Trailing `D` |

Активация ещё требует NAV ≥ $100 при дефолтах (`minAccountSize / maxSingleAccountBps`). Депозиты ниже этого работают.

## Выходы

Обычный `withdraw(shares)` сразу. `requestWithdraw` / `executeWithdraw` это опциональная пауза 1 день на том же учёте.

Тратить можно только кэш на EVM сверх резерва, возвратов и выплат. Утилизация после выхода должна остаться в лимите. Лока shares из-за открытых funded-счетов нет.

`deposit(amount, proposedReferrer)` может привязать referral LP при первом касании. `depositFor` никогда не привязывает.

## Управление

Коридоры размера счёта, drawdown и утилизации двигает `setRiskConfig`. Тиры комиссий и сплиты 50/50 это константы.

<Card title="Доход" icon="trending-up" href="/ru/vault/yield">
  Откуда доход волта и что может опустить цену share
</Card>
